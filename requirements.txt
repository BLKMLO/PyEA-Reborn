# Installation : `pip install -r requirements.txt` peut échouer PARTIELLEMENT
# sans erreur visible (pip installe paquet par paquet dans certains cas) —
# symptôme observé : uvicorn présent mais `click` absent au lancement. Cause
# typique : lightgbm/pyarrow sans wheel précompilé pour une version Python
# trop récente (ex. 3.13 sous Windows), qui échouent à la compilation et
# interrompent le reste de l'install. Deux solutions :
#   1. Utiliser Python 3.11 ou 3.12 (wheels garanties pour tous ces paquets).
#   2. Si 3.13 (ou plus récent) est imposé, installer un par un pour isoler
#      le paquet fautif :
#      pip install fastapi "uvicorn[standard]" jinja2 pydantic-settings PyYAML ^
#        SQLAlchemy ib_async pandas numpy pyarrow lightgbm pytest httpx

# Backend web
fastapi>=0.115
uvicorn[standard]>=0.30
jinja2>=3.1

# Configuration
pydantic-settings>=2.4
PyYAML>=6.0

# Stockage (SQLite au départ, Postgres plus tard sans réécriture)
SQLAlchemy>=2.0

# Broker Interactive Brokers (fork communautaire maintenu d'ib_insync)
ib_async>=1.0

# Broker MetaTrader 5 — paquet officiel, Windows UNIQUEMENT (IPC vers le
# terminal MT5). Non installable sous Linux/macOS (dev/CI), d'où l'import
# paresseux dans broker_metatrader.py : décommenter sur le poste Windows.
# MetaTrader5>=5.0.45 ; sys_platform == "win32"

# Stratégie (Couleuvre_v0.1 — logique développée plus tard)
lightgbm>=4.3

# Données historiques (download_history.py) & backtest
pandas>=2.2
numpy>=1.26          # features de Couleuvre (import direct, sinon transitif via pandas)
pyarrow>=16.0

# Moteur de backtest : backtrader (GPLv3, PUR PYTHON) — NON listé ici car
# VENDORISÉ dans lib/backtrader/ (zéro install, hors-ligne). pyea/__init__.py
# préfixe lib/ dans sys.path. Aucun pip requis. Pour le mettre à jour :
# remplacer le dossier lib/backtrader/ par une nouvelle version.

# Tests
pytest>=8.0
httpx>=0.27
